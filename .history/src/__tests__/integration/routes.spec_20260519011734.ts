// Playwright integration: every public route renders without dark-mode regressions.
// Placeholder; implementation lands with later tasks.

import { test } from "@playwright/test";

test.skip("routes render in Light_Theme without dark-mode classes", async () => {
  // TODO: implement in the routes integration task.
});
