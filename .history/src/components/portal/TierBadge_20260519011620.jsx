/**
 * @file src/components/portal/TierBadge.jsx
 * @description Reusable tier-state badge rendered next to gated controls in
 * the customer portal (e.g., locked products, upgrade banners, runtime
 * tiles).
 *
 * Subsequent tasks (see `tasks.md` § 10 — Portal tier-gating UX) will:
 *   - Render a small chip indicating current vs required tier.
 *   - Source colors exclusively from Theme_Tokens (no hard-coded hex).
 *   - Pair with `LockedScreen` / `LockedFeatureBlock` / `UpgradeBanner`
 *     copy and CTAs.
 *
 * This file is a deliberate placeholder — see Requirements 15.2 and
 * design.md § Tier-gating UX in the portal.
 */
