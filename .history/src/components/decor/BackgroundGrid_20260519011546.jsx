/**
 * @file src/components/decor/BackgroundGrid.jsx
 * @description Fixed full-viewport industrial grid rendered once at the App
 * root and persisted across routes.
 *
 * Subsequent tasks (see `tasks.md` § 4.2) will:
 *   - Render a single `<div>` with two `linear-gradient`s (no SVG) drawing
 *     1 px lines on a 32 px × 32 px lattice in `colors.border.subtle`.
 *   - Apply `position: fixed; inset: 0; pointer-events: none; z-index: 0`.
 *
 * This file is a deliberate placeholder — see Requirements 6.1, 6.7 and
 * design.md § Components and Interfaces > Industrial decor layer; § Property 13.
 */
