/**
 * Backwards-compatibility re-export.
 * All code should import from supabaseClient.js directly.
 * This file exists so existing imports don't break.
 */
export { supabase } from './supabaseClient'
