// Playwright integration: coarse-pointer devices skip hover-only interactions.
// Placeholder; implementation lands with later tasks.

import { test } from "@playwright/test";

test.skip("pointer: coarse skips magnetic and hover-only interactions", async () => {
  // TODO: implement in the pointer-coarse integration task.
});
