// Unit tests for src/hooks/useReducedMotion.js.
// Placeholder; implementation lands with later tasks.

describe("useReducedMotion", () => {
  it.skip("returns true when prefers-reduced-motion: reduce is the active media query", () => {
    // TODO: implement in the use-reduced-motion task.
  });
});
